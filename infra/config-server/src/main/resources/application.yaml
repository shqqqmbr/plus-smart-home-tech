server:
  port: 0

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations:
            - classpath:config/telemetry/{application}
            - classpath:config/commerce/{application}
            - classpath:config/infra/gateway/{application}

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/